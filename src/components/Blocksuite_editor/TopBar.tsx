import React from "react";

// Since the CSS module cannot be found, we will remove the import statement
// and use inline styles instead for demonstration purposes.

const TopBar: React.FC = () => (
  <div
    style={{ padding: "10px", backgroundColor: "#f0f0f0", textAlign: "center" }}
  ></div>
);

export default TopBar;
